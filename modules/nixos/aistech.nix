{
  config,
  lib,
  pkgs,
  ...
}:
{
  #services.unifi = {
  #  enable = true;
  #  openFirewall = true; # Automatically opens the necessary ports
  #  unifiPackage = pkgs.unifi; # Uses the stable version
  #};
}
